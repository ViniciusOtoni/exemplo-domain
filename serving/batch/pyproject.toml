[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "exemplo-serving-batch"
version = "0.4.1"
requires-python = ">=3.11"
# Pinado pela URL do WHEEL do Release, não por git+https: o wheel do domínio é
# instalado dentro do serverless, e satisfazer um git+ lá exigiria git e um clone.
dependencies = [
    "mlplatform @ https://github.com/ViniciusOtoni/platform-libs/releases/download/mlplatform-v3.7.4/mlplatform-3.7.4-py3-none-any.whl",
]

[project.entry-points."mlplatform.domains"]
exemplo_serving_batch = "exemplo_serving_batch.configs"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["src"]
